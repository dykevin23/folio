export { default } from "~/features/settings/pages/desktop";
export { loader } from "~/features/settings/queries";
export { action } from "~/features/settings/mutations";

export function meta() {
  return [{ title: "Folio — 설정" }];
}
